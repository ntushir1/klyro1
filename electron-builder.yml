# electron-builder.yml

# The unique application ID
appId: com.isotryon.klyro

# The user-facing application name
productName: Klyro

# Publish configuration for GitHub releases
publish:
    provider: github
    owner: isotryon
    repo: klyro
    releaseType: draft

# Protocols configuration for deep linking
protocols:
    name: IsotryonKlyro Protocol
    schemes: 
        - isotryonklyro

# List of files to be included in the app package
files:
    - src/**/*
    - package.json
    - klyro_web/backend_node/**/*
    - '!**/node_modules/electron/**'
    - public/build/**/*

# Additional resources to be copied into the app's resources directory
extraResources:
    - from: klyro_web/out
      to: out

asarUnpack:
    - "src/ui/assets/SystemAudioDump"
    - "**/node_modules/sharp/**/*"
    - "**/node_modules/@img/**/*"

# Windows configuration
win:
    icon: src/ui/assets/logo.ico
    target:
        - target: nsis
          arch: x64
        - target: portable
          arch: x64
    executableName: "Klyro"
    requestedExecutionLevel: asInvoker
    verifyUpdateCodeSignature: false

# NSIS installer configuration for Windows
nsis:
    oneClick: false
    perMachine: false
    allowToChangeInstallationDirectory: true
    deleteAppDataOnUninstall: true
    createDesktopShortcut: always
    createStartMenuShortcut: true
    shortcutName: Klyro
    installerIcon: src/ui/assets/logo.ico
    artifactName: "Klyro-Setup-${version}.${ext}"

# macOS specific configuration
mac:
    # The application category type
    category: public.app-category.utilities
    # Path to the .icns icon file
    icon: src/ui/assets/logo.icns
    # Minimum macOS version (supports both Intel and Apple Silicon)
    minimumSystemVersion: '11.0'
    hardenedRuntime: true
    entitlements: entitlements.plist
    entitlementsInherit: entitlements.plist
    target:
      - target: dmg
        arch: universal
      - target: zip
        arch: universal
